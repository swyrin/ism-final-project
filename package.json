{
  "name": "ism",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --if-present dev",
    "start": "pnpm -r --parallel --if-present start",
    "build": "pnpm -r --parallel --if-present build",
    "lint": "oxlint",
    "format": "oxfmt",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "oxfmt": "0.41.0",
    "oxlint": "1.56.0",
    "oxlint-tsgolint": "0.17.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.30.1"
}